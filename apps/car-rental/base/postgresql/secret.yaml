apiVersion: v1
kind: Secret
metadata:
  name: postgresql-credentials
  labels:
    app: postgresql
type: Opaque
stringData:
  # POC placeholder only — replace with Sealed Secrets / External Secrets before
  # this goes beyond a lab POC (see architecture doc, LLD 04).
  POSTGRES_USER: carrental
  POSTGRES_PASSWORD: carrental-poc-changeme
  POSTGRES_DB: carrental
